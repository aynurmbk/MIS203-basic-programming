# Bug Assignment – Hüseyin Talha Kaya

Your program: **`even_odd_counter.py`**

## What should the program do?

The program asks for numbers until the user types `done`. Then it prints how many even and how many odd numbers were entered.

## Your task

There are **3 bugs** in this program:

* 1 bug stops the program from starting (a syntax error),
* 1 bug crashes the program while it is running (an error message),
* 1 bug gives the wrong result (the program runs, but the output is wrong).

Find and fix all 3 bugs. Change **only** the lines that have bugs.

## Correct output

When the program works correctly, this is what you should see
(the values after the `:` are what the user typed):

```
Enter a number (done to finish): 4
Enter a number (done to finish): 7
Enter a number (done to finish): 10
Enter a number (done to finish): 3
Enter a number (done to finish): 8
Enter a number (done to finish): done
Even numbers: 3
Odd numbers: 2
```
